# 随读随译精简记录（2026-09-18）

## 当前行为

- 打开漫画后自动翻译；阅读窗口变化采用 450ms 防抖，只包含当前图片和后两张，按此顺序逐页提交。
- 普通账户跨模式、语言和设备最多 3 张在途，PLUS 最多 10 张。旧环境的 10/500 设置不能放大新上限；已有超限任务保留，等待释放后再新增。
- 满额或服务器排队在图内左上角显示等待动画，提交、上传与运行显示翻译动画；权益不足显示升级入口，弹窗保持当前阅读位置。
- 译图自动显示，原图可切回；已有结果自动匹配恢复。翻译失败可显式重试，未知请求保留原幂等键恢复，不因翻页重发。
- 移除队列入口／列表、选页批量翻译、整章预存、范围确认、持续补充、自动显示开关、手动恢复和常规额度展示；服务端计量与权限仍保留。

## 自动验证

在 `apps/extension` 运行 `npm run check`、`npm test`、`npm run build`、`npm run build:web`。类型和模块检查通过，243 项测试通过，Chrome MV3 与网页版均可构建。网页版仍有大于 500 kB 的包体提示。

在 `backend` 运行 `.venv/Scripts/python.exe -m pytest tests -q`。包含跨模式总容量、重复提交、取消释放、多设备争抢、权益与真实 HTTP 计算进程恢复；供应商及图像引擎使用隔离模拟，未调用真实模型。最终结果见本次交付消息。

## 手动验收

用户已确认自行完成浏览器验收，本次未取得浏览器截图：浏览器连接工具报请求头策略加载错误；computer-use 后因无法可靠识别当前浏览器 URL 停止，未继续操作。

重新加载 `apps/extension/.output/chrome-mv3` 并重启后端即可验收真实插件。优先检查快速翻页、满额等待后当前页优先补位、PLUS 累计 10 张、失败重试、翻译完成后锚点不变，以及升级入口。

无需真实供应商的辅助样本：在 `apps/extension` 执行 `npm exec vite -- --host 127.0.0.1 --port 5174 --strictPort`，打开以下独立测试来源，选择同名“自动翻译”漫画。页面右下角显示提交页码、在途数，并提供模拟完成／失败／权益恢复按钮。只操作此来源的测试数据，不复用实际服务令牌。

- [普通用户](http://127.0.0.1:5174/tests/reader-fixture.html?auto=free)
- [PLUS 用户](http://127.0.0.1:5174/tests/reader-fixture.html?auto=plus)
- [权益不足](http://127.0.0.1:5174/tests/reader-fixture.html?auto=quota)

代码与本地构建已交付，未提交 Git、推送或公开部署。
