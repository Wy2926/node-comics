# 生产审查修复与共享对象实现

2026-09-16。本次落实用户列出的 P1/P2、全局对象复用和移除一天后无引用对象清理；使用全新 `shared_0001` 空库基线，不提供旧数据库、旧对象键或旧接口迁移兼容。没有修改既有运行库、部署服务或提交 Git。原 [生产审查](PRODUCTION_REVIEW.md) 保留为修复前证据。

## 已实现

| 范围 | 当前行为 |
| --- | --- |
| 生产身份 | 默认生产模式，禁止 DEV_AUTH，要求完整 OIDC、PostgreSQL、私有 R2、精确网页与插件来源；生产引导使用独立项目/数据库/对象前缀，并校验最终生效环境 |
| 已确认身份值 | Client ID `dept2iz42nzidf5pao6fo`；issuer `https://auth.nodelane.net/oidc`；audience `https://comics.nodelane.net/api`，网站首页为 `https://comics.nodelane.net` |
| 公钥撤销 | 删除无限期单钥缓存，只保留有 TTL 的 JWKS 集合；到期刷新失败即拒绝，撤销窗口默认不超过 300 秒 |
| 首次登录 | 唯一约束冲突后回滚回查，按当前已验签 claims 同步角色 |
| 提交反滥用 | 跨副本数据库请求/条目预算、每日预算和并发限制；429 带重试时间；客户端保留幂等键等待后重试 |
| 回执保留 | 完整回执及关联任务完成均超过 30 天才归档条目及内部逐项请求，外部幂等墓碑永久保留，旧 key 返回 410 而非重执行 |
| 调度锁 | 全队列候选查询移到锁外，只在锁内复核有限候选；已验证原图的新提交只绑定当前回执的页面，不重扫历史清单 |
| 对象共享 | 原图与最终译图按字节 SHA-256 使用不可覆盖的公共内容对象键；已验证同图免上传，同模式/语言/配置的已完成译图跨账户免费复用 |
| 用户隔离 | 用户持有各自的 Asset/Job/FilePage；不泄露他人资源 ID、任务、文件名、角色或账本，删除个人授权不影响他人共享内容 |
| 恢复与保留 | 删除孤儿扫描、扫描表与生产对象列举逻辑；不按对象年龄或当前 DB 无引用删除；租约保存内容对象键，恢复重新校验 SHA 和可解码性 |
| 健康与恢复 | 存活/就绪分离、OIDC/执行/维护进展、队列告警、脱敏任务定位、Compose 健康检查；新增只允许隔离目标的数据库备份/恢复及对象引用清单 |

共享范围是已验证原图和**已完成**译图；同时提交但尚未完成的跨账户翻译仍独立执行，取消、未知费用和任务状态不交叉。重新翻译和更换有效模型配置可生成新版本。全局内容摘要匹配是已明确选择的产品行为，不是字节持有证明；公开接口始终校验当前账户自己的授权记录。

用户删除撤销的是个人授权。持久对象无引用时仍保留，临时上传也不再依赖孤儿扫描自动删除；物理容量管理和桶级灾备需独立运维策略。

## 配置与操作入口

- [生产身份与 Logto 配置](PRODUCTION_IDENTITY.md)：已将用户确认 audience 写入根 `.env`，生产文件在 Git 忽略的 `deploy/.env.production`。
- [提交保护与调度查询](SUBMISSION_SCHEDULING.md)：参数、归档语义、公平选举和压测方法。
- [对象存储](OBJECT_STORAGE.md)：全局字节去重、账户授权、条件写与存储边界。
- [健康与隔离恢复](OPERATIONS.md)：存活/就绪/告警、备份和恢复演练命令。

## 验证边界

数据库和权限测试使用临时 SQLite/专用 PostgreSQL 随机 schema，不调用付费模型。真实 R2 使用随机隔离前缀及两张合成图片，上游图片模型为模拟响应；原图/译图真实写入、可解码签名下载、第二账户免上传/免调用复用和条件写均通过，本次写入的 3 个对象已逐一删除并确认不存在。没有读取或修改产品图片对象。

真实 PostgreSQL 恢复演练已通过：2 个用户的 2 个授权对应 1 个共享对象引用，备份后的写入不进入快照，拒绝覆盖已存在目标库；备份不包含 R2 字节。实际独立 API/控制/维护/计算进程覆盖维护进程停止后 ready 503、live 200 和重启恢复 ready 200。

真实 OIDC 用户/管理员登录、生产反向代理、网关 IP 级保护、生产容量及桶级灾备仍需在目标环境验收。本次未改变原审查中的 R2 公开域名绑定，不能据代码测试推断该外部入口已关闭。

## 最终验证记录

- 后端完整回归：**451 passed，0 failed，0 skipped**，238.47 秒；启用专用 PostgreSQL 并发和五万页调度测试。两条警告来自现有 Starlette/httpx/anyio 测试依赖弃用提示。
- 扩展：`npm run check`、`npm test`（23 个文件、**231 项通过**）、`npm run build` 均通过。改动为提交错误处理与重试；未做浏览器视觉验收。
- 五万页队列：100 个用户、12 次领取，调度锁中位数 **34.8 毫秒**、最大 **49.1 毫秒**，每次最多载入 2 个 Job；不含锁外 SQL 耗时，不作为生产吞吐承诺。
- 真实 R2：合成原图/译图写入、签名下载解码、跨账户复用、条件写通过；模拟上游调用 1 次、付费调用 0 次，3 个测试对象清理后均确认不存在。
- 真实 PostgreSQL 备份恢复：新库恢复、快照一致性、共享引用清单与拒绝覆盖通过。真实 HTTP 进程测试覆盖维护退出/恢复与节点故障恢复。
- `git diff --check` 通过；新文档的本地链接已检查。没有 Git 提交、推送或公开部署。

本地证据位于被忽略的 `artifacts/production-fixes/`：`backend-tests-final.xml`、`scheduler-scale-final.json`、`r2-shared-smoke.json` 与 `restore-drill-final/report.json`。R2 报告的 `r2_failure_type` 是旧诊断代码记录的清理后预期 404，报告的 `status=passed` 和 `cleanup=verified_written_keys_absent` 表明验证及清理均成功；脚本已修正预期 404 的诊断分类。
