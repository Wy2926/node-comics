# 漫画管理交互改版

日期：2026-09-14。范围为扩展漫画管理、来源目录导入，以及 MangaCopy 动态图片清单发现。沿用现有本地漫画模型与存储，保留原图、版本和阅读位置。

## 交互

- 作品封面、章节、卷册、可读副本和来源目录均采用网格卡片；搜索、排序和筛选独立于编辑。
- 进入批量选择后显示选择工具，支持 Shift 连选；不再常驻起止条目表单和重复操作栏。
- 下载／补齐、授权、暂停、重试集中到采集中心。章节卡片保留阅读和版本入口。
- 版本选择显示当前选中状态，并在保存后反馈；编辑、归属、关联和清理通过按需弹窗或更多菜单进行。
- 出版套系不再作为常驻新建区域或卷册浏览的前置条件；保留已有数据关系与必要编辑。
- 写入过程禁用重复提交，成功提示可关闭且相同文案可再次触发；错误保留到用户关闭或下一次操作。

## 本地验证入口

在 `apps/extension` 启动 `npm run dev -- --port 5175`，打开 `/tests/library-fixture.html`；目录导入检查追加 `?catalog=1`。此页面只允许独立的 5175 端口，使用原创样本与隔离漫画数据，拒绝在已有非验收漫画库上初始化。

样本包含三部作品、十二章节、两卷、多内容版本、失败／暂停采集状态与五十条来源目录。保存、排序、阅读位置与版本切换使用实际组件和 IndexedDB。样本采集状态为模拟，不证明真实网站下载或原生权限弹窗可用；真实站点结果单独记录。

## 卡片布局统一（2026-09-15）

- 先移除旧列表布局、重复的书架行高规则和已废弃的行式卡片样式；书架、章节、卷册、副本、相关作品与插件译本改用共用 `CardBody`。
- 标识固定一行，标题固定两行，说明按预留行展示；空内容保留高度，长中文及无空格英文截断，完整文字保留在提示和可访问名称中。普通卡片保留两行说明，插件译本保留四行，包含处理／失败／失效状态。
- 操作区固定 36px 高、两列等宽；阅读在左、详情在右。没有本地原图时禁用阅读，详情仍可用；批量选择占用相同操作行。相关作品的查看／解除关联也固定在此行。
- 章节、卷册、副本和相关作品统一封面比例与网格；内容版本的计数／当前标记保留固定宽度。书架页预留滚动条空间，防止筛选或切换内容后网格列数跳动。390px 两列，低于 360px 单列。

复现：独立的 5175 端口访问 `/tests/library-fixture.html?cards=1`，补充长短标题、无空格英文、长来源／版本和缺封面样本；`?translations=1` 检查模拟的插件译本状态。只修改该验收库，不需要账户或供应商。样本入口不进入生产扩展。

本轮验证：类型与模块检查通过；漫画管理和目录／译本相关 21 项单元测试通过；Chrome MV3 构建通过。内置 Chromium 在 1440px、390px 下测量卡片高度、内容行和按钮相对坐标，并检查截图：同类卡片一致，缺图和不同状态没有撑高或挤压操作区。320px 书架／译本无横向溢出。已验证缺原图详情入口、批量选择、长版本名筛选、版本切换保存，以及返回书架后继续阅读仍为第 2 页。此轮为本地组件与构建验收，未进行商店发布或真实翻译效果验收。

## 卷册收录与关联（2026-09-15）

- 卷册详情的展开区显示收录与关联数量；收录按顺序展示编号卡片，卷册关系分为合订收录、再版来源两个分组，附类型图标与卷册卡片。未记录的内容有独立空状态；窄屏改为单列，长标题换行。
- 两种编辑弹窗显示当前整理的卷册、候选卡片和已选数量。当前作品没有章节或其他卷册时，明确说明原因及导入／纠正副本归属的下一步，禁用保存，提供返回卷册详情按钮。
- 保存、取消和关闭编辑均返回已展开的卷册详情，恢复展开区的焦点与可见位置。

沿用上面的独立 5175 端口入口。Chrome 152 隔离浏览器已验证空选项、选择保存、取消不落库、刷新持久化、循环关系报错及返回详情；检查了 1440px、390px 和深色截图，长标题无横向溢出。`npm run check` 与 Chrome MV3 构建通过。此轮未调用翻译供应商，未发布扩展；截图、检查脚本与结果保存在被 Git 忽略的 `artifacts/publication-details`。

## 本次证据

- 全量单元测试：16 个文件、142 项通过，包含 8 项采集专项检查。
- 最终 `npm run check` 通过，58 个源模块无运行时循环或不可达模块；`npm run build` 生成 Chrome MV3 扩展。
- 浏览器交互：书架名称排序、章节倒序、Shift 连选三项并标记已读、内容版本切换与当前标识、优先阅读副本、采集入队与暂停反馈已验证。
- 续读：从版本卡片打开优先副本，跳到第 2 页，返回书架后继续阅读仍为相同副本第 2 页。
- 最终视觉：已检查书架、章节、版本弹窗与采集中心桌面截图，390px 章节／卷册网格、目录导入和采集中心截图；卡片操作无重叠、页面无横向溢出。采集面板 349px 可用宽度与滚动宽度一致。
- 目录导入：来源倒序、Shift 连选、搜索空结果时保留其他筛选的已选项、三条新章节实际保存、390px 窄屏无横向溢出已验证。
- 真实 MangaCopy 清单：第 73 話 43/43（约 42.7 秒）、第 1 卷 166/166（约 165.9 秒）、第九卷番外 27/27（约 26.6 秒），均在非活动来源标签页从 4 个链接继续发现。弹窗直接发现番外也为 27/27。
- 真实原图：在新版采集中心下载番外 27/27 页，解码并持久化；下载中重启、两个阅读器并存恢复、首尾页阅读通过，没有页面异常或翻译请求。

清单和下载恢复的脱敏记录分别位于 `artifacts/mangacopy-validation/discovery-results.json` 与 `results.json`；浏览器交互截图位于 `artifacts/library-redesign`。这些目录被 Git 忽略。采集代理的截图来自 CSS 中间构建，不作为最终布局证据。

真实站点验证使用隔离扩展配置，预先授权已观察到的来源与 CDN。原生浏览器权限弹窗、所有章节／CDN 和登录／验证码场景不在此次验收结论内。没有在日常浏览器配置中安装扩展，没有调用翻译供应商，没有提交 Git、推送或发布。
