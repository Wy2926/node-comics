[project]
name = "node-comics-package-build"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = ["setuptools==84.0.0", "wheel==0.45.1", "pytest==8.4.2", "cryptography==50.0.1"]

[tool.uv]
package = false
environments = ["sys_platform == 'win32' and platform_machine == 'AMD64'"]
