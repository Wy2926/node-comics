// Public configuration, never put a client secret or access token here.
// Replace values only in your deployed HTTPS authorization site.
globalThis.NODE_COMICS_DRIVE_CONFIG = Object.freeze({
  clientId: '',
});
