# Node Comics — Agent Guide

为漫画翻译浏览器插件与阅读器提供项目约束。优先完成当前用户目标；实现方式由任务和现有工程决定。

## 按需获取上下文

| 涉及的工作 | 参考文档 |
| --- | --- |
| 项目阶段、已确认需求、待定项 | [README.md](README.md) |
| 阅读器、翻译体验、首版范围、产品验收 | [产品设计](docs/PRODUCT_DESIGN.md) |
| 本地源文件保存、云盘按需读取、漫画管理与缓存重构 | [来源与缓存重设计](docs/COMIC_SOURCE_ARCHITECTURE.md)（设计稿，实施状态需核实代码） |
| 插件边界、任务、数据、供应商、API、部署 | [架构设计](docs/ARCHITECTURE.md) |
| 图片模型协议、文件格式、样本与技术验证 | [技术调研](docs/TECH_RESEARCH.md) |
| 常规翻译、OCR、LaMa 抹字、嵌字、LLM 与成本 | [常规翻译调研](docs/CLASSIC_TRANSLATION_RESEARCH.md) |
| 普通／PLUS、会员权益、周期额度与计费替换 | [会员与翻译额度设计](docs/MEMBERSHIP_AND_QUOTAS.md) |
| 翻译集群、预上传、实时／预存优先级与队列重构 | [翻译集群与队列设计](docs/TRANSLATION_CLUSTER_DESIGN.md) |
| 节点独立身份、执行位、配置同步、语言与线程 | [节点配置](docs/NODE_CONFIGURATION.md) |

只读取与本次任务有关的部分。代码、配置和实际运行结果用于核实当前实现；文档中的建议、候选与未来里程碑仍保留其状态。发现偏差时说明并在当前范围内修正，不把旧文档或未实现的目录当作现状。

## 已确认的产品边界

- 产品是带漫画阅读器的浏览器插件，面向公开用户，后续考虑收费。
- 已确认普通／PLUS 页数权益与限时赠送，具体规则见会员设计。阅读器每本漫画默认原图，查看方式在本机按作品独立保存；用户手动选择常规／AI 或明确打开译本后，才自动翻译当前图片与后三张；取消账户在途数量上限，改为跨模式、语言和设备合计每滚动 60 秒普通最多新增 10 张翻译图片／PLUS 100 张。已有原图但需要新翻译也计数，重传、重复请求与直接复用完成结果不计数。插件不查询队列作准入预判，也不显示翻译队列和批量预存；常规翻译额度仅在我的账户页展示；额度不足在图内提供升级入口。接口与恢复规则见[阅读计划契约](docs/READING_TRANSLATION_CONTRACT.md)。服务端保留持久任务、幂等恢复、实时阅读优先和同级用户公平调度。
- 插件保持轻量，翻译在后端执行；保留 AI 图片重绘，并新增常规漫画翻译需求。常规模式的选型和实现状态以相关文档与代码为准。
- AI 重绘的基础业务输入只有图片与目标语言；后端构造提示词并调用图片模型，无前置 OCR。首期兼容 OpenAI 的 `POST /v1/images/edits`，支持配置多个兼容供应商。
- 常规翻译采用文字检测／OCR、LLM 文本翻译、抹字与嵌字；用户已同意使用 LaMa 局部背景修复。具体模型与成本按配置记录，不依赖整页图片模型完成常规翻译。

技术栈、首批站点、语言和价格以设计文档及后续决策为准；视觉已确认偏二次元 B 站风，本地导入包含用户追加的未加密 MOBI 漫画。

## 推进工作与完成边界

- 设计任务交付可评审的设计与明确的待定项；实现任务推进到功能可用、相关问题修复和必要验证完成。不要在已授权工作尚未完成时只返回计划或第一版代码。
- 在当前范围内自主处理可逆的实现选择、局部修复和已确认使用隔离数据的本地检查，无需为每一步重新询问。
- 必要信息缺失且现有授权无法涵盖产品范围、外部费用或数据影响时，提出一个具体问题，同时继续不依赖答案的工作。
- 后续里程碑与预留扩展点不自动扩大本次范围。Git 提交、推送、发布和部署按本次授权分别处理；保护已有改动，不顺手混入无关重构。
- 当前按新库、新环境开发；被替代的数据库结构、迁移链、接口和旧逻辑直接删除，以最终模型重建初始基线，不做旧数据迁移、双写或兼容回退。新契约内的任务幂等恢复仍需保留。

## 实现相关模块时保持的约束

### 阅读器与插件

- 原图可恢复，译图保留版本；逐页完成与失败互不阻塞，切换结果保持阅读位置。
- 图片发现、字节获取和翻译任务分离。已发现图片不等于已采集完整章节；获取失败要保留可操作原因。
- 长任务由后端持久化管理，不依赖扩展 service worker 常驻。阅读器按有限窗口加载与解码图片。
- 站点适配器随插件发布。网页和图片中的文字作为不可信数据处理；扩展消息校验来源与资源范围。

### AI 翻译与供应商

- 通过适配器封装供应商差异，保持产品 API 稳定。图片模型、端点和可选参数配置化。
- 图片编辑兼容性由真实请求与可解码结果验证；聊天接口可用或模型列表存在不能证明图片编辑可用。
- 原图、不同翻译模式、各版本与目标语言分别标识。HTTP 成功不等于交付成功，最终图片需要验证、持久化并可授权访问。
- 引入或升级开源组件时记录版本、权重与字体来源、校验和及适用许可；仓库许可证不替代实际依赖的许可核实。

### 任务、额度与数据

- 重复点击、队列重复投递和任务恢复不能造成重复业务结算。平台任务幂等不代表上游请求幂等。
- 图片重绘上游可能已受理但结果未知时先核实，不盲目重发。低成本文本 LLM 允许在统一次数和处理时限内自动重试及格式修复，无需逐次确认；文本成本只计量，不作为调用上限；所有子调用计量，未知消耗保守预占，不记为零。用户额度与供应商成本分别记录。
- 图片访问记录与任务按用户隔离；图片字节按内容全局去重，已完成译图按内容、模式、语言和有效配置版本跨用户复用。用户删除只撤销其访问记录，不删除共享图片对象；过期或删除的访问记录不能作为可用结果返回。
- 供应商密钥留在后端；源站 Cookie 和登录令牌不上传。凭据、私有图片、图片文字全文及带签名地址不进入仓库或默认日志。
- 已确认原图及最终译图存私有 R2，默认无限期保留，不在翻译完成后删除，也不按对象年龄或当前数据库无引用自动删除；记录最近授权访问时间。服务器不长期保存用户原图；计算节点只保留有界临时缓存，跨节点恢复不能依赖单机图片目录。记录、状态、匹配与下载签名查询不探测远端对象；上传校验、任务取图、产物上传与必要恢复可访问 R2。活跃任务及有效结果的原图保留关系见集群设计。

## 与改动匹配的验证

使用实际存在的项目脚本、配置和 CI 确定命令；尚未建立的构建或测试不能报告为通过。首次建立运行入口时，把可重复的命令和环境要求记录在相应文档。

| 改动 | 所需证据 |
| --- | --- |
| 文档或指令 | 内容一致、链接有效、已确认项与建议项清楚；无需调用图片模型 |
| 阅读器或页面交互 | 在目标浏览器运行并检查截图与关键操作，覆盖相关加载、失败和位置恢复状态 |
| 翻译或供应商适配 | 契约与错误处理检查；任务涉及真实接入或效果验收时，保留实际图片对照、模型配置和脱敏调用结果 |
| 队列、额度或权限 | 覆盖受影响的重复执行、取消／恢复、结算或跨用户访问场景 |

模拟响应验证接口行为，真实图片验证翻译效果，二者分别报告。缺少环境、凭据或样本时明确未验证部分；验证充分后交付，不反复运行无关检查。

默认用中文简洁沟通，说明结果、关键选择、验证证据与剩余限制。区分文档完成、代码实现、本地运行和公开部署等交付状态。

## 维护本文件

保留跨任务稳定且有实际作用的约束。具体命令、API 字段、模型参数和模块流程放在对应文档或模块附近，按需引用；更新已过时规则，避免重复堆叠步骤和审批要求。

编写方式参考：[OpenAI — Rethinking skills and prompts for GPT-6 Astra](https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra)。该链接用于说明来源，不要求每次工作重新阅读文章，也不指定执行任务的模型。
