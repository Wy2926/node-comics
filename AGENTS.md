# Node Comics — 开发约定

带漫画阅读器的桌面浏览器插件。先从 [README](README.md) 找到相关模块，只读取当前任务所需文档，以代码、配置和运行结果核实现状。

## 项目边界

- 一本漫画绑定一个来源，导入直接阅读；网站目录只读。支持范围见[产品设计](docs/PRODUCT_DESIGN.md)，不加入散图导入、资料编辑、多来源或版本管理。
- 来源发现、图片读取、翻译任务分离。站点逻辑留在各自适配器中，遵循[网站适配规范](docs/SITE_ADAPTERS.md)，公共层不加站点分支。
- 官方翻译由后端持久化执行，插件保持有限图片窗口；原图可恢复，逐页失败互不阻塞，切换结果保持位置。行为以[翻译契约](docs/READING_TRANSLATION_CONTRACT.md)为准。
- 任务和访问授权按用户隔离，重复执行不能重复结算。未知图片调用先核实；供应商成本与用户额度分开计量。原图、译图按[存储规则](docs/OBJECT_STORAGE.md)保留。
- 密钥留后端，源站 Cookie／令牌不上传；凭据、私有图文和签名地址不进入仓库或默认日志。第三方代码、模型与字体保留来源、版本、摘要和许可。

## 工作与验证

- 完成已授权范围内的实现、修复与必要验证，保护已有改动；提交、推送、发布和部署按用户授权处理。
- 使用模块现有脚本。交互改动检查桌面浏览器操作、截图、失败与位置恢复；队列、额度、权限改动覆盖重复执行、恢复、结算与隔离。
- 分别说明模拟测试、真实服务、模型效果与部署结果；未验证的部分明确标注。默认中文简洁交付。
- 文档只维护当前规范：README 提供用途、运行和导航；专题文档维护唯一规则。删除被替代的方案、过程记录和重复说明，不新增日期型报告。文档改动检查链接与一致性即可。
